config {
    type: 'table',
    database:'secure-potion-409606',
    schema:'harika_dataset',
    name: 'orgtbl',
    tags:['newone']
}

select t1.* from ${ref("info1")} as t1 inner join ${ref("info2")} as t2 on t1.id=t2.id
